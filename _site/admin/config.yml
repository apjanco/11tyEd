backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)

publish_mode: editorial_workflow
media_folder: "_site/assets" # Media files will be stored in the repo under images/uploads
